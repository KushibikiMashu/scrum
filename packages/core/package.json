{
  "name": "@panda-project/core",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc --noEmit && tsc --watch",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "jest": "jest"
  },
  "devDependencies": {
    "@panda-project/config": "*"
  },
  "private": true
}
